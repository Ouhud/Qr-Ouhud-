# =============================================================================
# 🔐 Ouhud QR - Environment Variables
# =============================================================================
# WICHTIG: ENCRYPTION_KEY muss gesetzt werden für QR-Inhalt-Verschlüsselung!
# Der Key muss exakt 32 Bytes (64 Hex-Zeichen) lang sein.
# =============================================================================

# --- Database ---
DATABASE_URL=sqlite:///./qr.db

# --- Session Secret ---
SESSION_SECRET=ouhud-session-secret-change-in-production
SESSION_COOKIE_NAME=ouhud_session
SESSION_SAME_SITE=lax
SESSION_HTTPS_ONLY=0

# --- Encryption Key for QR Content (AES-256-GCM) ---
# ⚠️ DIESER KEY IST KRITISCH FÜR DIE SICHERHEIT!
# Er muss 32 Bytes = 64 Hex-Zeichen lang sein.
# Beispiel-Key (diesen durch einen eigenen, zufälligen Key ersetzen!):
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# --- SMTP/E-Mail (optional) ---
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-password

# --- App Settings ---
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=true

# --- Stripe Billing ---
# Test keys from Stripe Dashboard (Developers -> API keys)
STRIPE_PUBLIC_KEY=pk_test_xxx
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_DOMAIN=http://127.0.0.1:8000

# Optional: use fixed Stripe Price IDs per plan (recommended)
STRIPE_PRICE_BASIC=price_xxx_basic
STRIPE_PRICE_PRO=price_xxx_pro
STRIPE_PRICE_BUSINESS=price_xxx_business

# Comma-separated emails that should never be charged (owner/founder accounts)
BILLING_EXEMPT_EMAILS=founder@example.com

# Comma-separated domains that should never be charged (e.g. internal company accounts)
BILLING_EXEMPT_DOMAINS=ouhud.com
